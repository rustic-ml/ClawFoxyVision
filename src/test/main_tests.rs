#[cfg(test)]
mod tests {
    #[test]
    fn include_tests() {
        println!("Running main_tests::include_tests");
        // This is just a dummy test to ensure the tests are included
        assert!(true);
    }
}
